# Copy to .env and fill in real values for the client environment.
# The defaults in docker-compose.yml are safe for local dev only.
# Never commit a .env file with real credentials.

POSTGRES_DB=appdb
POSTGRES_USER=appuser
POSTGRES_PASSWORD=apppassword
